# MVP Requirements Entry Point

[中文](mvp-requirements.cn.md) | [English](mvp-requirements.md)

Requirements have moved to the Chinese-only [SPEC.md](../../SPEC.md). The original 24 `ZMCP-MVP-*` identifiers remain unchanged under “需求与验收追踪” (requirements and acceptance traceability); HTTP/HTTPS and public interface requirements are maintained in the same specification.

This page preserves the previous link and does not maintain a separate table. Update the specification and [coverage matrix](../testing/requirements-coverage.md) when requirements change. See the [MCP tools reference](../reference/mcp-tools.md) for parameter details.
